package com.dreammap.app.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Purple Colors (Purple & Black Theme)
val DarkPurple = Color(0xFF6B46C1)       // Primary: Main purple for buttons, primary elements
val MediumPurple = Color(0xFF9333EA)     // Secondary: Medium purple for secondary elements
val LightPurple = Color(0xFFC084FC)      // Tertiary: Light purple/lavender for accents
val VeryLightPurple = Color(0xFFE9D5FF)  // Very light purple for backgrounds

// Black Colors
val PureBlack = Color(0xFF000000)        // Pure black
val NearBlack = Color(0xFF0A0A0A)         // Near black for backgrounds
val DarkBlack = Color(0xFF1A1A1A)        // Dark black for surfaces
val Charcoal = Color(0xFF2A2A2A)         // Charcoal for elevated surfaces

// Dark Theme Backgrounds (Purple & Black)
val DarkPurpleBackground = Color(0xFF0A0A0A)  // Near black background
val DarkPurpleSurface = Color(0xFF1A1A1A)     // Dark black for cards/surfaces
val DarkPurpleElevated = Color(0xFF2A2A2A)    // Charcoal for elevated surfaces

// Gamification Colors
val Gold = Color(0xFFFBBF24)            // Gold for achievements, XP, badges
val LightGold = Color(0xFFFCD34D)       // Light gold for highlights
val Pink = Color(0xFFEC4899)           // Pink for energy, excitement, special events
val LightPink = Color(0xFFF472B6)       // Light pink for accents

// Neutral Colors
val White = Color(0xFFFFFFFF)           // Text, contrast
val LightGray = Color(0xFFE0E0E0)       // Background for cards/surfaces in light theme
val DarkGray = Color(0xFF1F2937)        // Dark gray for text in light theme
val MediumGray = Color(0xFF4A4A4A)      // Medium gray for secondary text